let Constants = {
  // Set default text color to black with maximum opacity.
  defaultTextColor: '#000000FF',

  // Disabled text color is the same color, but different opacity.
  disabledTextColor: '#00000060'
};

let Styles = {

};

export { Constants, Styles };
